# 依赖
node_modules/
**/node_modules/

# 临时部署脚本（含明文密码，禁止入库）
deploy_final.py
deploy_upload.py
server_deploy.js
*_deploy.py
deploy_remote.*

# 构建产物（提交到仓库，服务器不用构建）
# web/frontend/dist/
# web/frontend/.vite/

# 临时文件
*.log
tmp/
C:\tmp/
coverage/
.nyc_output/

# 编辑器
.vscode/
.idea/
*.swp
*.swo
*~

# 系统
.DS_Store
Thumbs.db

# 环境变量（含密钥，不入库）
.env
.env.local

# 运行时设置（含 LLM API Key 等密钥，不入库）
data/settings.json

# 运行时漏洞数据（用户扫描结果，不入库）
data/findings.json
web/server/data/

# 上传的文件（用户数据）
C:/tmp/avh-uploads/
C:/tmp/avh_src_*

# 临时解压目录
avh_src_*/
avh-uploads/

# Python 缓存
__pycache__/
*.pyc

# package-lock（可选，大型项目建议保留，这里保留以锁定版本）
# package-lock.json
